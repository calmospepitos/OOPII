<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TP1" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/starbucksgreen</item>
        <item name="colorPrimaryVariant">@color/starbucksgreen</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/starbucksgreen</item>
        <item name="colorSecondaryVariant">@color/starbucksgreen</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="chipStyle">@style/StyleChip</item>
    </style>

    <style name="StyleChip" parent="Widget.MaterialComponents.Chip.Choice">
        <item name="chipBackgroundColor">@color/starbucksgreen</item>
        <item name="android:textColor">@color/white</item>
        <item name="checkedIcon">@drawable/crochet</item>
        <item name="checkedIconVisible">true</item>
    </style>
</resources>